// YAML entry point (`@maroonedsoftware/appconfig/yaml`). Importing this module
// pulls in the `yaml` peer dependency; the core entry does not.
export { AppConfigSourceYaml } from './sources/app.config.source.yaml.js';
