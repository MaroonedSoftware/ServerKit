---
'@maroonedsoftware/utilities': minor
---

Add `Array` and `String` prototype extensions (`unique`, `cast`, `deleteProperties`, `intersect`, `compare`, `binarySearch`, `takeWhile`, `takeWhileAggregate`, `isNullOrUndefinedOrWhitespace`, `hasValue`, `mask`, `maskExceptLastFour`, `maskEmail`) and the `joinNonEmpty` helper. Extensions install on import and only register when the prototype method is not already defined.
